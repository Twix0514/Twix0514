# Twix0514

This repository contains a simple Python application entrypoint in `app.py`.

Usage:

```powershell
python app.py --greet YourName
```

You can also override or set `ADMIN_PATH`:

```powershell
setx ADMIN_PATH "your_custom_admin_path"
python app.py --status
```
